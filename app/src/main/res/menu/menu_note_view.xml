<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/edit_note"
        android:title="@string/edit"
        android:icon="@drawable/ic_notebook_edit"
        app:showAsAction="ifRoom"/>

    <item android:id="@+id/delete_note"
        android:title="@string/delete"
        android:icon="@drawable/ic_delete"
        app:showAsAction="ifRoom"/>

    <item android:id="@+id/pin_note"
        android:title="@string/pin"
        android:icon="@drawable/ic_not_pinned"
        app:showAsAction="ifRoom"/>

    <item android:id="@+id/lock_note"
        android:title="@string/lock"
        android:icon="@drawable/ic_unlocked"
        app:showAsAction="ifRoom"/>

    <item android:id="@+id/note_info"
        android:title="@string/info"
        android:icon="@drawable/ic_info"
        app:showAsAction="ifRoom"/>

    <item android:id="@+id/share_note"
        android:title="@string/share"
        android:icon="@drawable/ic_share"
        app:showAsAction="ifRoom"/>
</menu>