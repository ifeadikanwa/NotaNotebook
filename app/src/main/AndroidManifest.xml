<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.notanotebook">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/short_app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ArchiveActivity"
            android:screenOrientation="portrait"
            android:parentActivityName=".NotebookActivity">

            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".NotebookActivity" />
        </activity>
        <activity
            android:name=".NoteViewActivity"
            android:screenOrientation="portrait"
            android:parentActivityName=".NotebookContentActivity">

            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".NotebookContentActivity" />
        </activity>
        <activity
            android:name=".ChecklistEditActivity"
            android:screenOrientation="portrait"
            android:parentActivityName=".NotebookContentActivity"
            android:theme="@style/ChecklistEditActivityTheme">

            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".NotebookContentActivity" />
        </activity>
        <activity
            android:name=".NoteEditActivity"
            android:screenOrientation="portrait"
            android:parentActivityName=".NotebookContentActivity"
            android:theme="@style/NoteEditActivityTheme">

            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".NotebookContentActivity" />
        </activity>
        <activity
            android:name=".NotebookContentActivity"
            android:label="Notebook Content"
            android:screenOrientation="portrait"
            android:parentActivityName=".NotebookActivity">

            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".NotebookActivity" />
        </activity>
        <activity
            android:name=".TestActivity"
            android:screenOrientation="portrait"
            android:theme="@style/ChecklistEditActivityTheme">

        </activity>
        <activity android:name=".NotebookActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>